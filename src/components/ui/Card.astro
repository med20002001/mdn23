---
type Props = {
  class?: string;
};

const { class: className = "" } = Astro.props;
---

<div class={`bg-white/80 backdrop-blur rounded-2xl shadow-sm ring-1 ring-black/5 ${className}`}>
  <slot />
</div>
