---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import partenariatsImg from "../assets/images/partenariats.jpg";
---

<Layout title="Partenariats">
  <section class=" py-14 sm:py-20">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="bg-white rounded-2xl shadow-md p-6 sm:p-10 lg:p-14">

        <!-- Header -->
        <header class="mb-16 max-w-3xl">
          <h1 class="mt-2 text-3xl sm:text-4xl lg:text-5xl font-extrabold text-gray-900">
            Partenariats
          </h1>

          <div
            class="h-1 w-20 rounded-full mb-6
                   bg-gradient-to-r from-red-500/40 to-green-500/40"
          ></div>

          <p class="mt-6 text-gray-600 text-base sm:text-lg">
            Plongez également dans nos Partenariats, tissés avec d'autres
            organisations et initiatives partageant nos valeurs, afin
            d'amplifier notre impact collectif.
          </p>
        </header>

        <!-- Image -->
        <div class="relative w-full aspect-[16/9] rounded-2xl overflow-hidden shadow-xl">
          <Image
            src={partenariatsImg}
            alt="Partenariats MDN23"
            class="w-full h-full object-cover"
            loading="lazy"
            widths={[640, 960, 1280]}
            sizes="100vw"
          />
          <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
        </div>


      </div>
    </div>
  </section>
</Layout>
