---
import Layout from "../layouts/Layout.astro";

const article = {
  title: "Rencontre avec Nasrine Kheltent",
  date: "27 juillet 2023",
  image: "/images/team/nasrine.jpg",
  href: "#", // plus tard: lien vers la page de détail
};
---

<Layout title="News">
  <main class="bg-white">
    <section class="max-w-6xl mx-auto px-4 lg:px-0 py-16">
      <!-- Titre section -->
      <h1 class="text-2xl md:text-3xl font-bold text-gray-900 mb-6">
        News
      </h1>

      <!-- Carte news principale -->
      <article class="bg-white shadow-sm border border-gray-200 rounded-lg overflow-hidden">
        <div class="grid grid-cols-1 md:grid-cols-2">
          <!-- Colonne image -->
          <div class="relative">
            <img
              src={article.image}
              alt={article.title}
              class="w-full h-full object-cover"
              loading="lazy"
            />
          </div>

          <!-- Colonne texte -->
          <div class="p-6 md:p-8">
            <p class="text-xs uppercase tracking-wide text-gray-500 mb-2">
              Publié le {article.date}
            </p>

            <a
              href={article.href}
              class="text-base md:text-lg font-semibold text-gray-900 underline underline-offset-2 decoration-gray-800 hover:text-black"
            >
              {article.title}
            </a>

            <p class="mt-4 text-sm text-gray-700 leading-relaxed">
              Artiste belgo-marocaine, Nasrine tisse des liens entre les scènes
              culturelles de son pays d’origine et de son pays d’accueil, à
              travers des projets mêlant image, son et narration contemporaine.
            </p>

            <p class="mt-3 text-sm text-gray-700 leading-relaxed">
              Ses créations explorent l’espace urbain, la mémoire et les
              identités multiples, en donnant une place centrale à la parole
              des personnes rencontrées sur le terrain.
            </p>
          </div>
        </div>
      </article>
    </section>
  </main>
</Layout>
